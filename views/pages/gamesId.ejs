<%- include('../partials/header') %>
  <main>
    <h1><%= myGame.game_name %></h1>
    <p>Game ID: <%= myGame.game_id %></p>

    <% if(locals.myGenres) { %>
    <div class="list-genres">
      
        <p>Genres:</p>
        <% myGenres.forEach(genre => { %>
          <div>
            <a href="/games?genres=<%=genre.genre_name.toLowerCase() %>"><%= genre.genre_name %></a>
          </div>
        <% }); %>
    </div>
    <% } %>

    <% if(locals.myDeveloper) { %>
    <div class="container-developer">
      <p>Developer: <a href="/games?developers=<%= myDeveloper.developer_name %>"><%= myDeveloper.developer_name%></a></p>
    </div>
    <% } %>

    <a href="/games/<%= myGame.game_id %>/edit">
      <button>Edit</button>
    </a>
    <form action="/games/<%= myGame.game_id %>" method="POST">
      <input type="hidden" name="_method" value="DELETE">
      <button type="submit">Delete</button>
    </form>
  </main>
<%- include('../partials/footer') %>